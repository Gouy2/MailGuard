# 2026-05-10 Phase 5C 测试日志

## 目标

扩充 mock 邮件数据集，并确认 deterministic evaluation、tool runtime 和 LLM shadow eval 入口不被破坏。

## 结果

通过。

## 命令记录

```bash
python3 -m unittest tests.test_email_tools
```

结果：

```text
22 tests OK
```

```bash
python3 -m py_compile server/app/*.py server/evaluate_email.py client/aemeath/*.py tests/test_email_tools.py
```

结果：通过。

```bash
python3 server/evaluate_email.py --classifier rule --limit 36
```

关键结果：

- `sample_count`: 36
- `category_accuracy`: 1.0
- `importance_accuracy`: 1.0
- `action_accuracy`: 1.0
- `important_recall`: 1.0
- `important_precision`: 1.0
- `noise_filter_precision`: 1.0
- `mismatches`: 0
- `errors`: 0

```bash
uv run python evaluate_email.py --classifier llm --limit 6 --continue-on-error
```

关键结果：

- `model`: `deepseek-v4-flash`
- `sample_count`: 6
- `category_accuracy`: 0.6667
- `importance_accuracy`: 0.8333
- `action_accuracy`: 0.8333
- `important_recall`: 1.0
- `important_precision`: 1.0
- `noise_filter_precision`: 1.0
- `errors`: 0

LLM mismatch 摘要：

- `email-036`: social noise 被分为 notification，但仍保持 low/ignore，不影响 reportability。
- `email-035`: finance invoice 被分为 action_required，但仍保持 high/review，不影响 reportability。
- `email-032`: meeting reminder 被分为 high/pay_attention，安全边界仍正确，但 action/importance 偏激进。

结论：

- Phase 5C 数据集扩充完成。
- deterministic baseline 在 36 条 mock 邮件上全对。
- LLM shadow eval 链路可用，但下一步需要分析 label taxonomy 和 prompt 约束，尤其是 `finance` vs `action_required`、`noise` vs `notification`、near-term meeting 的 importance/action。
